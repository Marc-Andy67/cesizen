{% extends 'admin/base_admin.html.twig' %}

{% block admin_content %}
    <div class="mb-6 border-b pb-4 flex justify-between items-center">
        <h1 class="text-2xl font-bold text-gray-800">Modifier : {{ quiz.title }}</h1>
        <a href="{{ path('app_admin_quiz_index') }}" class="text-gray-500 hover:text-gray-700 transition-colors">
            &larr; Retour à la liste
        </a>
    </div>

    <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-6 max-w-3xl">
        {{ include('admin/quiz/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
{% endblock %}
