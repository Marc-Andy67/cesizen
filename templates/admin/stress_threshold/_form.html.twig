{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="form-control w-full">
            {{ form_label(form.level, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.level) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.level) }}</div>
        </div>

        <div class="form-control w-full">
            {{ form_label(form.name, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.name) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.name) }}</div>
        </div>

        <div class="form-control w-full">
            {{ form_label(form.minScore, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.minScore) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.minScore) }}</div>
        </div>

        <div class="form-control w-full">
            {{ form_label(form.maxScore, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.maxScore) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.maxScore) }}</div>
        </div>
        
        <div class="form-control w-full md:col-span-2">
            {{ form_label(form.description, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.description) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.description) }}</div>
        </div>

        <div class="form-control w-full md:col-span-2">
            {{ form_label(form.advice, null, {'label_attr': {'class': 'label font-bold'}}) }}
            {{ form_widget(form.advice) }}
            <div class="text-error text-sm mt-1">{{ form_errors(form.advice) }}</div>
        </div>
    </div>

    <div class="mt-6 flex justify-end gap-2">
        <a href="{{ path('app_admin_stress_threshold_index') }}" class="btn btn-ghost">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
