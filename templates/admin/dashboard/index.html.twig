{% extends 'admin/base_admin.html.twig' %}

{% block admin_content %}
    <h1 class="text-3xl font-bold text-gray-800 mb-6">Tableau de Bord Administrateur</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        {# Stat Card Utilisateurs #}
        <div class="bg-blue-50 border border-blue-100 p-6 rounded-lg shadow-sm">
            <h3 class="text-blue-500 font-semibold mb-2">Total Utilisateurs</h3>
            <p class="text-4xl font-bold text-blue-900">{{ total_users }}</p>
        </div>
    </div>
{% endblock %}
